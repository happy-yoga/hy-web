extends layout

block content
  include components/index

  h1= page.title

  if page.contentElements
    each contentElement in page.contentElements.entries
      if contentElement.contentTypeId
        +#{contentElement.contentTypeId}(contentElement)


  //- pre
    = JSON.stringify(page, null, 2)
