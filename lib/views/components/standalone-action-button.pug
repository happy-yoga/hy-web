mixin standaloneActionButton(entry)
  standalone-action-button
    a.button(href=entry.link.url class=(entry.link.external ? 'external-link' : '') target=(entry.link.external ? "_blank" : false) )
      if entry.link.external
          span.icon
      = entry.link.linkLabel
